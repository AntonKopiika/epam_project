language: python
python:
  - 3.8.5
install:
  - pip install -r requirements.txt
  - pip install coveralls
script:
  - coverage run --source=src -m pytest && coverage html

notifications:
  email:
    reciplients:
      - toxxa099@gmail.com
    on_success: never
    on_failure: always

after_success:
  - coveralls